div.container
    div.row(ng-repeat="track in tracks.list")
        div.col-xs-12
            | {{($index + 1)}}. {{track.name}} (#[a(ng-href="#/artists/{{track.artist.name}}") {{track.artist.name}}])
